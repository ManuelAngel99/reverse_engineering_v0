/**
 * Type Definitions (Legacy Export)
 *
 * @deprecated Import from specific modules instead:
 * - @/core/devtools/types
 * - @/core/runtime/types
 * - @/core/compiler/types
 * - @/core/router/types
 * - @/core/fonts/types
 * - @/core/http/types
 * - @/dns/types
 *
 * Or import all from @/core/models
 */

// Re-export all core types for backward compatibility
export * from "@/core/models";

// Re-export DNS types
export * from "@/dns/types";

